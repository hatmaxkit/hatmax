version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "assets/migration/postgres"
    gen:
      go:
        package: "sqlcgen"
        out: "internal/sqlcgen"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
